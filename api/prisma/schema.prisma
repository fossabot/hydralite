datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum AttachmentType {
  image
  video
  other
}

enum OauthConnectionService {
  github
  google
  discord
  twitter
}

model User {
  id String @id @default(uuid())
}