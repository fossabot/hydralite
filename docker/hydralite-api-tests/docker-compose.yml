services:
  db:
    image: postgres:13
    restart: always
    container_name: hydralite-api-tests
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: hydralite
      POSTGRES_PASSWORD: hydralite
      POSTGRES_DB: hydralite-tests-db
